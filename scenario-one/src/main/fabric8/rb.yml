apiVersion: "v1"
kind: "RoleBinding"
metadata:
  name: "qs-scenario-one-config"
roleRef:
  # Add the view role to the service account
  name: "view"
subjects:
  - kind: "ServiceAccount"
    name: "qs-scenario-one-config"