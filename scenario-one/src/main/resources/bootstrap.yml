# Startup configuration of Spring-cloud-kubernetes
spring:
  application:
    name: scenario-one-config
  cloud:
    kubernetes:
      reload:
        # Enable live reload on ConfigMap change (disabled for Secrets by default)
        enabled: true
      secrets:
        paths: /etc/secrets/scenario-one-config